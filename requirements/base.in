open-api-framework[cors,markup,geo,csp,commonground,inclusions,sanitization,server,redis,structlog]

psycopg[pool]

# Django packages
django-capture-tag
django-localflavor
mozilla-django-oidc-db[setup-configuration]

maykin-common[axes,mfa,otel,vcr]

# Extra
pytz
jsonschema[format]

# Framework libraries
django-jsonsuit
